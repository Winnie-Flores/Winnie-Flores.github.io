---
title: first-post
---

# h1
变量命名：字母，数字，下划线，美元符号。but数字不能开头，不能JavaScript自带的名称作变量名。
primitive data types:
  Number
  String 引号
  Boolean: true/false
  Undefined 
  Null
  Symbol
  Biglnt
  
   typeof null返回object,这是JavaScript的一个错误，注意避开
#声明变量
let 声明变量，更改时不同再写let；
const声明变量不可更改，不可声明空变量；（尽量用这个，若无需更改）
避免使用var；
#模版字符串#
用反引号` ${}`引用变量用${}
用引号表示字符串时，换行用\n\
但是用模板文字，可以直接换行写
#emoji
CMD+CTRL+space
#类型转换
字符串转数字：Number( )
若是非数字，返回NaN（一个无效数字），所以typeof NaN,返回number
数字转字符串：String（ ）
#类型强制
字符串和+一起用，数字转成字符串
和-，* ，/ 一起用，字符串强制转数字
  #虚假值
  0，‘ ‘，undefined ,null,NaN在布尔值中是false
  空字符串也是true
  



## h2

## haha

*nihao*
**good morning**